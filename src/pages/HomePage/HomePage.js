import React, { Fragment } from 'react';
import Grid from '@material-ui/core/Grid';

export const HomePage = props => {
    return (
        <Fragment>
            <Grid container>
                
            </Grid>
        </Fragment>
    );
};